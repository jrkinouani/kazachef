<div class="container">
  <div class="row">
    <% @plats.each do |plat| %>
    <h2><%= link_to plat.title.capitalize,plat %></h2>
    
    <% end %>
    <%= link_to " Ajouter un plat", new_plat_path %>
  </div>
</div>
